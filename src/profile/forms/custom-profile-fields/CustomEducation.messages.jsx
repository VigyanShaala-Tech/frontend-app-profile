import { defineMessages } from '@edx/frontend-platform/i18n';

const messages = defineMessages({
  'profile.custom.education.empty': {
    id: 'profile.custom.education.empty',
    defaultMessage: 'Add level of education',
    description: "instructions when the user doesn't have their level of education set in custom profile",
  },
});

export default messages;
